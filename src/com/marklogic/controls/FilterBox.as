/* ----------------------------------------------------------------------	Copyright 2002-2010 MarkLogic Corporation.  All Rights Reserved.---------------------------------------------------------------------- */package com.marklogic.controls {		import flash.display.Sprite;	import flash.text.TextField;	import flash.geom.ColorTransform;	import flash.geom.Transform;	public class FilterBox extends Sprite {				// Constants:		// Public Properties:		public var _labelField:TextField;		public var _bg:Sprite;		// Private Properties:		protected var _index:Number;		protected var _data:Object;		protected var _label:String		// UI Elements:		public var colorTrans:ColorTransform;		public var trans:Transform;			// This is the transform of the MovieClip (displayChip) used to show the currently selected color. 				// Initialization:		public function FilterBox(p_value:Object) {			_data = p_value;			label = (!_data.lex) ? _data.name : _data.lexType+": "+_data.name;						// transform of the clip used to display the color selected			trans = new Transform(_bg);			// Create the ColorTransform that does all of the Color changing			colorTrans = new ColorTransform();			configUI();		}		// Public Methods:		public function set label(value:String):void {			_label = (value.indexOf(':') != -1) ? value.substring(value.indexOf(' ')+1,value.length) : value;			_labelField.text = value;		}		public function get label():String {			return _labelField.text;		}				public function get data():Object {			return _data;		}				public function set index(p_value:int):void {			_index = p_value;			if(_index != 0) { label = _data.lexType+": "+_label; }		}		public function get index():int { return _index; }				public function set bgColor(p_color:uint):void {			colorTrans.color = p_color;			trans.colorTransform = colorTrans;		}				// Protected Methods:		// Private Methods:		protected function configUI():void {			mouseChildren = false;			useHandCursor = true;			buttonMode = true;		}	}	}